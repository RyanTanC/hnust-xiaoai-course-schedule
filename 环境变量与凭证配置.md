# 环境变量与凭证配置

本项目**不在源码中保留任何个人或应用凭证**。所有敏感值通过 `.env` 文件或环境变量注入。

---

## 一、`.env` 配置（必需）

复制模板并填写：

```bash
cp .env.example .env
```

编辑 `.env`：

```ini
# 小米开放平台应用凭证（用于密码登录 / 扫码登录签发 Access Token）
XIAOMI_CLIENT_ID=你的CLIENT_ID
XIAOMI_CLIENT_SECRET=你的CLIENT_SECRET

# 服务端口（可选）
PORT=8080
```

> `.env` 已被 `.gitignore` 屏蔽，**不会**进入 Git 仓库。
> 未配置时程序仍能启动，但密码登录 / 扫码登录会在签发 Token 阶段报错并提示缺失项。

### 如何查看有没有配好

启动后访问 `GET /api/config`：

```json
{
  "has_app_credentials": true,   // ← 这个字段
  "has_userinfo": true,
  "has_token": true
}
```

`has_app_credentials` 为 `false` 说明 `.env` 没配好或没被读到。

---

## 二、`data/userinfo.txt` 配置（二选一登录方式）

这是**个人账号凭证**，不是应用凭证。适合不想走扫码流程、直接导入令牌的场景。

### 获取方式

1. 在手机或电脑上登录「小爱课程表」（小米 AI 课表）
2. 用抓包工具（Charles / Fiddler / mitmproxy）抓 `https://i.xiaomixiaoai.com` 的请求
3. 复制请求头里的 `authorization` 字段值
4. 参考 `data.example/userinfo.txt.example` 的格式写入 `data/userinfo.txt`

### 格式

```json
{
  "authorization": "DO-TOKEN-V1 app_id:...,scope_data:...,access_token:...",
  "userAgent": "你的设备 UA（从抓包结果复制，用于设备指纹伪装）",
  "userId": 0,
  "deviceId": "",
  "sid": "i.ai.mi.com"
}
```

最小可用字段只有 `authorization`，其余可选。

### 过期行为

`authorization` 令牌**会过期**（通常数天到数周）。过期后的表现：

- `/api/login/userinfo` 返回 `401` + `code: token_expired`
- 前端自动登录失败，提示「缓存的登录凭证已失效，请重新登录」

**处理方式**：重新抓包替换 `data/userinfo.txt`，或在界面点「扫码登录」重新获取。

---

## 三、敏感文件清单（全部已被 `.gitignore` 屏蔽）

| 文件 | 内容 | 风险等级 |
|---|---|---|
| `.env` | 小米开放平台应用凭证 | 🔴 高 |
| `data/userinfo.txt` | 个人账号 authorization 令牌 | 🔴 高 |
| `data/aischedule_config.json` | `saved_token` / 设备 UUID | 🔴 高 |
| `data/.secret_key` | Flask session 签名密钥 | 🟡 中 |
| `data/xskb_list.do.txt` | 从教务系统抓取的原始课表 | 🟡 中 |
| `_private_backup/` | 脱敏前的私有文件备份 | 🔴 高 |
| `*.log` | 运行日志（可能含请求详情） | 🟡 中 |

---

## 四、开源前自查清单

提交代码前跑一遍：

```bash
# 1) 确认 .env 和 data/ 没被追踪
git status --porcelain --untracked-files=all

# 2) 确认这些文件都在 .gitignore 里
git check-ignore -v .env data/userinfo.txt data/aischedule_config.json data/.secret_key

# 3) 全仓库搜残留的 token 特征（应无结果）
#    注意：把下面两个模式拆开写，避免本文档自身被匹配到
git grep -n "TOKEN-V1 app_id:[0-9]"
git grep -n "access_token:V" 

# 4) 确认示例文件里是占位符而非真值
git grep -n "XIAOMI_CLIENT_SECRET=" -- .env.example    # 应为空值
```

> 第 3 步之所以拆成两条，是因为单条命令里同时出现完整模式串时，
> 本文档自己就会被搜到，产生假阳性。

---

## 五、机制说明

`.env` 由 `core.load_dotenv()` 在模块导入时自动读取，**不依赖 python-dotenv**（零额外依赖）。

优先级：**命令行环境变量 > `.env` 文件**（`.env` 默认不覆盖已存在的环境变量）。

读取逻辑见 `core.py` 的 `load_dotenv()` / `get_xiaomi_credentials()`。
