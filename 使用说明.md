# 启动方式

## 方式零：先配置凭证（首次使用必须做）

复制 `.env.example` 为 `.env`，填写小米开放平台凭证：

```bat
copy .env.example .env
```

```ini
XIAOMI_CLIENT_ID=你的CLIENT_ID
XIAOMI_CLIENT_SECRET=你的CLIENT_SECRET
```

> **不配会怎样**：程序仍能启动、能看课表，但**扫码登录 / 密码登录会在签发 Token 阶段失败**，
> 并提示缺少哪两个环境变量。
>
> 完整说明见 `环境变量与凭证配置.md`。

## 方式一：一键启动（推荐）

双击 `run.bat` —— 会自动检查依赖、启动服务、打开浏览器。

## 方式二：直接用已备好的虚拟环境

```bat
cd /d "D:\Desktop\项目\教育工具\HNUST小爱课程表-XiaoAi"
.venv\Scripts\python.exe app.py
```

本项目 `.venv` 已预装全部依赖（Python 3.14.3 + Flask 3.1.3 + Playwright + openpyxl + waitress + bs4 + lxml），**无需重新装依赖**。

## 方式三：Docker

```bat
docker compose up
```

---

# 访问地址

http://127.0.0.1:8080

---

# 凭证与安全（开源脱敏）

本项目**不在源码中保留任何个人或应用凭证**。

| 文件 | 内容 | 是否进仓库 |
|---|---|---|
| `.env` | 小米开放平台应用凭证 | ❌ 已忽略 |
| `.env.example` | 占位符模板 | ✅ |
| `data/userinfo.txt` | 个人账号 authorization 令牌 | ❌ 已忽略 |
| `data.example/userinfo.txt.example` | 占位符模板 + 抓包教程 | ✅ |
| `data/aischedule_config.json` | saved_token / 设备 UUID | ❌ 已忽略 |
| `data/.secret_key` | Flask session 密钥 | ❌ 已忽略 |

`data/` 目录下**除 `.gitkeep` 外全部被忽略**。想确认配置是否生效，
访问 `/api/config` 看 `has_app_credentials` 字段是否为 `true`。

开源前请先跑一遍 `环境变量与凭证配置.md` 的「开源前自查清单」。

---

# 功能说明：作息时间微调 & 学期名称

## 一、课表名称按学年自动推算

「同步设置」区的「课表名称」下拉框会按当前日期自动推算学期：

- 9 月及以后 → 学年从本年开始（如 2026-09 → `26-27-1`）
- 1~8 月 → 学年从上年开始（如 2026-03 → `25-26-2`）

格式为 `学年前两位-学年后两位-学期序号`，例如：

| 名称 | 含义 |
|---|---|
| `26-27-1` | 2026-2027 学年 · 上学期（秋季） |
| `26-27-2` | 2026-2027 学年 · 下学期（春季） |
| `25-26-2` | 2025-2026 学年 · 下学期 |

- 「+ 新建」按钮也改用同一套学期选择器，不再弹裸输入框
- 同步时若选择了名称，会一并写入云端（相当于重命名）；选「（保持原名称）」则不动
- 单表「同步」按钮**不会**套用重命名，避免误改其他课表名称

## 二、🔍 同步预览（点同步前先看清改什么）

**痛点**：以前点「同步」是盲操作 —— 不知道传到哪个课表、会删掉什么。

现在有两个入口：

- **设置区**的「🔍 预览变更」按钮 → 预览"当前课表"
- **课表列表每一行**的「预览」按钮 → 预览那一行对应的课表

预览弹层会明确告诉你：

| 区块 | 内容 |
|---|---|
| 将同步到 | 目标课表的**名称**、是否当前课表、**云端 ID**、现有课程数 |
| 统计 | 将新增 N / 将删除 N / 无变化 N |
| 明细 | 逐条列出新增和删除的课程（名称、教师、地点、星期、节次） |
| 耗时预估 | 按 6 并发估算，如"预计约 3 秒" |
| 风险提醒 | 若删除量 ≥ 目标课表 80%，会警告"请确认没有选错课表" |

弹层底部直接给「确认同步（N 项变更）」按钮，点一下就走，不用再回到面板。

## 三、作息规则（本校参数化）

「作息规则（本校）」面板把作息表参数化了，不再写死时间：

| 参数 | 本校默认值 |
|---|---|
| 单节时长 | **45 分钟** |
| 小课间（组内） | **10 分钟** |
| 大课间（换教室） | **20 分钟** |
| 每组连上 | **2 节** |
| 冬季 下午起（第 5 节） | **14:30** |
| 冬季 晚修起（第 9 节） | **19:30** |
| 夏季 下午起（第 5 节） | **14:00** |
| 夏季 晚修起（第 9 节） | **19:00** |

改完点「保存规则」→ 节次时间立即按规则重算；点「恢复本校默认」回到上表。

面板底部实时显示验算：

```
单组时长      45×2 + 10×1 = 100 分钟
单组 + 大课间  120 分钟
```

### 生成结果（本校默认规则）

**冬季作息**

```
第 1节  08:00 ~ 08:45      第 7节  16:30 ~ 17:15
第 2节  08:55 ~ 09:40      第 8节  17:25 ~ 18:10
第 3节  10:00 ~ 10:45      ─── 晚饭 ───
第 4节  10:55 ~ 11:40      第 9节  19:30 ~ 20:15
─── 午休 ───               第10节  20:25 ~ 21:10
第 5节  14:30 ~ 15:15      第11节  21:30 ~ 22:15
第 6节  15:25 ~ 16:10      第12节  22:25 ~ 23:10
```

**夏季作息**：第 5 节起 `14:00`，第 9 节起 `19:00`，其余规则相同。

## 四、作息时间逐节微调

「作息时间微调（逐节）」面板：

- 节数由**智能推荐**决定，也可手动调 2~16
- 每节可单独改开始/结束时间
- 时间格式校验：必须是 `HH:MM`（24 小时制），且开始 < 结束，非法会标红并回滚
- 微调结果在点「同步到云端」时随课表设置一起写入云端（`sections` 字段）
- 服务端二次校验：若前端传来的节次非法，**静默回退**到季节预设，不会中断同步

## 五、✨ 智能作息推荐

点「✨ 智能推荐」，系统读你课表里**真实占用的节次**来反推：

1. 解析每门课的 `sections`（如 `1,2` / `5,6` / `9,10`），得出实际用到的小节序号
2. 按**上面那套本校规则**生成候选作息表
3. 总节数**收敛到真实需求**：你的课表最多用到第 10 节 → 推荐 10 节，而不是无脑补满 12 节

实测（本项目真实课表 39 门课）：

```
依据课表实际占用：共 10 个节次被课程使用，最大到第 10 节；推荐 10 节。
按本校规则铺排（45 分钟/节，2 节连上，小课间 10 分钟，大课间 20 分钟），
第 5 节 14:30、第 9 节晚修 19:30 起。

  第 1节  08:00 ~ 08:45      第 6节  15:25 ~ 16:10
  第 2节  08:55 ~ 09:40      第 7节  16:30 ~ 17:15
  第 3节  10:00 ~ 10:45      第 8节  17:25 ~ 18:10
  第 4节  10:55 ~ 11:40      ─── 晚饭 ───
  ─── 午休 ───               第 9节  19:30 ~ 20:15
  第 5节  14:30 ~ 15:15      第10节  20:25 ~ 21:10
```

> 数据来源：优先用本地捕获的课表（免登录态、秒出）；本地没有则回退云端已选课表。
> 推荐依据会显示在面板上方的蓝色提示条里，可随时手动改。

## 六、同步性能改进

原实现是同步阻塞 + 逐门课程串行发 HTTP（20 门课最坏要几分钟且无反馈）。现已改为：

| 改进点 | 说明 |
|---|---|
| 异步任务 | `/api/sync` 立即返回 `taskId`，前端轮询 `/api/sync/progress` |
| 分阶段进度 | 准备 → 设置 → 删除 → 上传，实时显示 `已完成/总数` 与进度条 |
| 并行批量 | 增/删各以 6 并发执行（原为串行） |
| 连接池复用 | 全局 `requests.Session` + 16 连接池 + 自动重试，省掉每次请求的 TLS 握手 |
| 列表增量渲染 | 刷新课表列表不再整体重建 DOM，展开态保留，且不再重复请求详情接口 |

---

# 常见问题

## 页面样式/脚本没更新（改了代码但界面还是旧的）

资源带版本参数 `?v=`，改前端后需同步 bump：

- `templates/index.html` 第 7 行（CSS）与最底部（JS）
- `static/js/app.js` 里的 `bumpAssetVersions()` 常量

当前版本：`20260918c`。硬刷新一次（Ctrl+F5）即可。

## 同步卡在「进行中」不动

进度轮询是每秒一次。若服务端任务线程异常退出，界面会显示「失败」。
可直接查 `flask_debug.log` 里的 `Sync failed` 或 `Sync completed` 行。

## 提示 "未检测到登录状态" 或反复 401

`data/userinfo.txt` 里的 token 已过期。解决办法：

1. 打开 http://127.0.0.1:8080
2. 选择「扫码登录」
3. 手机小米账号扫码
4. 扫码成功程序会自动关闭浏览器并完成登录

> 登录成功后凭证会双写：Flask session + 前端 `localStorage['aischedule_token']`。
> 若服务频繁重启导致 session 失效，前端会自动用 localStorage 的 token 无感恢复。

## 首次运行卡在 "检查 Playwright"

需要联网下载 Playwright chromium（约 130 MB）。已下载则跳过。

## 端口 8080 被占用

```bat
netstat -ano | findstr :8080
```

找到 PID 后在任务管理器结束，或修改 `app.py` 里的端口配置。

---

# 迁移说明

本目录由 `D:\xiaomi mutual transfer\Xiaomi Cloud\drive\短视频\XiaoAi湖南科技大学.exe_extracted\webapp` 迁出。

原解包目录 404 MB，其中约 271 MB 是 PyInstaller 打包的 Python 运行时与第三方库（`python313.dll`、`PYZ.pyz`、`numpy`、`matplotlib`、`PyQt5` 等），与项目源码无关，**未一并迁移**。

迁移后保留：

- `webapp/` 全部源码与数据
- `webapp/.venv/` 虚拟环境（131.85 MB，保证开箱即用）

迁移后体积：404 MB → 132 MB
