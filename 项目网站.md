# HNUST小爱课程表 - XiaoAi湖南科技大学

来源: 自研项目（非 GitHub 克隆）

搬迁时间: 2026-09-18 12:50:00

原始位置: D:\xiaomi mutual transfer\Xiaomi Cloud\drive\短视频\XiaoAi湖南科技大学.exe_extracted\webapp

---

## 项目简介

湖南科技大学教务系统（小爱课程表 / 小米 AI 课表）对接工具。

- 通过 Playwright 自动登录小米账号 / 扫码登录
- 拉取 HNUST 教务课表数据
- Flask 本地服务 + 纯前端 SPA，端口 8080
- 支持课表导出

## 技术栈

| 层 | 技术 |
|---|---|
| 后端 | Flask + waitress |
| 自动化 | Playwright (chromium) |
| 前端 | 原生 JS + CSS（无框架） |
| 解析 | beautifulsoup4 + lxml |
| 导出 | openpyxl |

## 目录说明

```
HNUST小爱课程表-XiaoAi/
├── 项目网站.md            # 本文件
├── 使用说明.md            # 用户使用指南
├── 环境变量与凭证配置.md   # ★ 凭证配置 + 开源前自查
├── app.py                 # Flask 服务入口 + API 路由 + capture worker
├── core.py                # 核心逻辑：登录、扫码、课表抓取
├── requirements.txt       # Python 依赖
├── .env.example           # ★ 环境变量模板（复制为 .env 并填写）
├── .gitignore             # ★ 屏蔽 data/ .env 等敏感路径
├── run.bat                # 一键启动（自动装依赖 + 起服务）
├── 启动.bat / 启动web.bat   # 备用启动脚本
├── DEPLOY.md              # 部署说明
├── Dockerfile             # 容器化
├── docker-compose.yml
├── data/                  # ★ 运行时数据（已被 gitignore，含个人凭证）
│   └── .gitkeep
├── data.example/          # ★ 配置模板（进仓库）
│   └── userinfo.txt.example
├── static/                # 前端资源
│   ├── css/style.css
│   └── js/app.js
├── templates/index.html   # 主页面
├── .venv/                 # Python 3.14 虚拟环境（已预装全部依赖）
├── _archive/probe_scripts/ # 历史调试探针脚本（不参与运行）
└── _private_backup/       # ★ 脱敏前私有文件备份（已被 gitignore）
```

> ★ 标记为「开源脱敏」相关文件，详见 `环境变量与凭证配置.md`

## 启动方式

双击 `run.bat`，浏览器自动打开 http://127.0.0.1:8080

或手动：

```bat
cd /d "D:\Desktop\项目\教育工具\HNUST小爱课程表-XiaoAi"
.venv\Scripts\python.exe app.py
```

**首次使用需先配置凭证**：复制 `.env.example` 为 `.env` 并填写小米开放平台凭证，
详见 `环境变量与凭证配置.md`。

## 注意事项

- **凭证不硬编码**：小米开放平台应用凭证通过 `.env` / 环境变量注入
- `data/userinfo.txt` 中的 token 会过期，过期后需重新扫码登录
- 登录成功后的凭证同时写入 Flask session 和前端 localStorage（`aischedule_token`）
- 首次运行需联网下载 Playwright chromium（约 130 MB），已下载则跳过
- **开源前**请先跑一遍 `环境变量与凭证配置.md` 里的自查清单

